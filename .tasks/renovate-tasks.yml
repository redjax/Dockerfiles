---
version: "3"

vars:
  RENOVATE_IMG_TAG: '{{.RENOVATE_IMG_TAG | default "latest"}}'
  RENOVATE_LOG_LEVEL: '{{.RENOVATE_LOG_LEVEL | default "info"}}'

tasks:
  extract:
    desc: "Extract dependencies using Renovate."
    cmds:
      - |
        set -euo pipefail

        REPO_ROOT="$(git rev-parse --show-toplevel)"

        echo "==> Extracting dependencies using Renovate"
        echo "    Repository: $REPO_ROOT"
        echo "    Renovate image: renovate/renovate:{{.RENOVATE_IMG_TAG}}"
        echo "    Log level: {{.RENOVATE_LOG_LEVEL}}"
        echo

        docker run --rm \
          -e RENOVATE_GITHUB_COM_TOKEN \
          -e LOG_LEVEL={{.RENOVATE_LOG_LEVEL}} \
          -v "$REPO_ROOT:/repo" \
          -w /repo \
          renovate/renovate:{{.RENOVATE_IMG_TAG}} \
          --platform=local \
          --dry-run=extract

  validate:
    desc: "Validate the local Renovate configuration."
    cmds:
      - |
        set -euo pipefail

        REPO_ROOT="$(git rev-parse --show-toplevel)"

        echo "==> Validating Renovate configuration"
        echo "    Repository: $REPO_ROOT"
        echo "    Renovate image: renovate/renovate:{{.RENOVATE_IMG_TAG}}"
        echo "    Log level: {{.RENOVATE_LOG_LEVEL}}"
        echo

        docker run --rm \
          -e LOG_LEVEL={{.RENOVATE_LOG_LEVEL}} \
          -v "$REPO_ROOT:/repo" \
          -w /repo \
          renovate/renovate:{{.RENOVATE_IMG_TAG}} \
          --platform=local \
          --dry-run=extract \
          --config-validation-error=true

  lookup:
    desc: "Look up available Renovate updates without making changes."
    cmds:
      - |
        set -euo pipefail

        REPO_ROOT="$(git rev-parse --show-toplevel)"

        echo "==> Looking up Renovate updates"
        echo "    Repository: $REPO_ROOT"
        echo "    Renovate image: renovate/renovate:{{.RENOVATE_IMG_TAG}}"
        echo "    Log level: {{.RENOVATE_LOG_LEVEL}}"
        echo

        docker run --rm \
          -e RENOVATE_GITHUB_COM_TOKEN \
          -e LOG_LEVEL={{.RENOVATE_LOG_LEVEL}} \
          -v "$REPO_ROOT:/repo" \
          -w /repo \
          renovate/renovate:{{.RENOVATE_IMG_TAG}} \
          --platform=local \
          --dry-run=lookup

  dry-run:
    desc: "Alias for the Renovate lookup task."
    cmds:
      - task: lookup
