# bump-my-version

Lightweight container image for versioning workflows. Uses the [`bump-my-version` CLI tool](https://github.com/callowayproject/bump-my-version).

Included tools:

- bump-my-version
- git
- curl
- pipx

Base image:

- alpine
