---
name: debian-base
category: base
description: Minimal Debian base image with initial tooling installed.
publish: true
context: dockerfiles/base/debian
dockerfile: dockerfiles/base/debian/Dockerfile
registry_path: ghcr.io/redjax/dockerfiles/debian-base
upstream:
  registry: docker
  name: debian
  track: 12
  version: "12.13"
version_args:
  DEBIAN_TAG: "12.13"
args:
  DEBIAN_TAG: "12.13"
