# gitlab-astro

Opinionated GitLab CI image for Astro site builds.

Included tools:

- docker CLI
- bash/coreutils/curl/git
- nodejs/npm
- python3/pip/pipx
- bump-my-version

Use this when you explicitly want an all-in-one pipeline image.
