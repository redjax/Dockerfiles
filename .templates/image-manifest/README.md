# Image.yml Manifest Template

Template for the `image.yml` manifest file accompanying each Dockerfile in this repository. Read more about image manifests in [the Dockerfile docs](../../docs/DOCKERFILES.md).
