---
name: Run Renovate

on:
  push:
    branches:
      - tmp/renovate-test
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Renovate mode"
        required: false
        default: "lookup"
        type: choice
        options:
          - extract
          - lookup
          - run
      log-level:
        description: "Renovate log level"
        required: false
        default: "info"
        type: choice
        options:
          - info
          - debug
          - trace

permissions:
  contents: read
  pull-requests: write
  issues: write
  actions: write

jobs:
  renovate:
    # uses: redjax/pipelinetemplates/.github/workflows/renovate.yml@3bf418a504882351e8a06541527d98ddf685cd6e # main
    uses: redjax/pipelinetemplates/.github/workflows/renovate.yml@feat/renovate-base-branch
    with:
      # mode: ${{ github.event_name == 'schedule' && 'run' || inputs.mode }}
      mode: run
      log-level: ${{ inputs.log-level || 'info' }}
      repository: ${{ github.repository }}
      config-file: renovate.json
      ## Set to 'ignored' while testing renovate.json changes on a different base branch,
      #  otherwise use 'optional'
      require-config: ignored
      autodiscover: false
      runner-image: ubuntu-latest
      renovate-author-email: "5534031+redjax@users.noreply.github.com"
      base-branches: "tmp/renovate-test"
      use-base-branch-config: true
    secrets:
      renovate-token: ${{ secrets.RENOVATE_TOKEN }}
      gh-api-token: ${{ secrets.GH_API_TOKEN }}
